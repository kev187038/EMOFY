pipeline {
    agent any
    stages {
        stage('mvn-build') {
            steps {
                bat 'mvn clean package'
            }
        }
        stage('docker-compose') {
            steps {
                bat 'docker-compose up'
            }
        }
    }
}
